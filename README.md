PHP-GET-POST-CRUD

This single-page PHP Student Management System uses the GET method to check URL parameters via isset() and retrieve student records from $_SESSION to display in the UI. It incorporates the POST method through AJAX to handle data submission, reading form values via $_POST to perform create, update, and delete actions dynamically without full page reloads.